import { KnowledgesPipe } from './knowledges.pipe';

describe('KnowledgesPipe', () => {
  it('create an instance', () => {
    const pipe = new KnowledgesPipe();
    expect(pipe).toBeTruthy();
  });
});
