import { WATTranslateLoader } from './wattranslate-loader';

describe('WATTranslateLoader', () => {
  it('should create an instance', () => {
    expect(new WATTranslateLoader()).toBeTruthy();
  });
});
